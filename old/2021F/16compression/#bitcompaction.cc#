void pack3(int* dest, int bits[], uint32_t len) {
	// if you really wanted to, you could put 2 bits of the last 3 bit number
	// into each word, and then onto the next
	// 3 3 3 3 3 3 3 3 3 3 2     1 3 3 3 3 3 3 3 3 3 3 1    2 3 3 3 3 3 3 3 3 3 3
	
}

int main() {
	int bits[] = {0, 7, 5, 2, 6, ...};
	int out[4];
	pack3(out, bits, sizeof(bits)/sizeof(int));

	// what would happen if the value is NOT an even number of bits?

	int val[] = {0, 5, 9, 2, 6, 3, 4, 4, 8, 1...};

	// arithmeticencoding() is the answer
	int sum = 0;
	// 9*100 8*10 2*1
	sum = sum * 10 + d;
	// ((0*10 + 5) * 10 + 9)* 10
	uint64_t a[1000];
  // 'h' - 'a'
	//'A' = 65 0x41
	//"hello there"
	// 7 4 11 11 14 26 19 7 4 17 4