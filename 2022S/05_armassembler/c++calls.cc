#include <iostream>

int f(int);
uint64_t g(uint64_t a, uint64_t b);

  double hypot(double a, double b);
  int64_t bigger(int64_t, int64_t);
int main() {
   int y = f(2);

   uint64_t ans = g(2,3);

   double d = hypot(3.0, 4.0);
   int64_t e = bigger(3,5);
}

