#include <iostream> // not stdio.h, that's old c style
#include <cmath> // not math.h, that's old c style

int loop1(int n) {
    int sum = 0;
    for (int i = 1; i <= n; i++)
        sum += i;
}


int loop1(int n) {
    int sum = 0;
    for (int i = 1; i <= n; i++)
        sum += i;
}
